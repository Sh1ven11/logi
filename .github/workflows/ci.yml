name: Logi CI

on:
  push:
    branches: [ Main ] # Ensure this matches your actual branch name (Main vs main)
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mariadb:
        image: mariadb:10.6
        env:
          MYSQL_ROOT_PASSWORD: admin
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=5s --health-timeout=2s --health-retries=3
      redis:
        image: redis
        ports:
          - 6379:6379

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install System Dependencies
        run: |
          sudo apt update
          sudo apt install -y mariadb-client curl build-essential
          curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
          sudo apt install -y nodejs
          sudo npm install -g yarn

      - name: Install Frappe Bench
        run: pip install frappe-bench

      - name: Initialize Bench
        # Fixed the flag name here
        run: |
          bench init --skip-redis-config-generation --frappe-branch version-15 test-bench
          cd test-bench
          bench get-app erpnext --branch version-15
          # Correct India Compliance Link
          bench get-app https://github.com/resilient-tech/india-compliance.git --branch version-15
          # Your Logi app
          bench get-app logi https://github.com/Sh1ven11/logi.git --branch Main

      - name: Create Site and Install Apps
        run: |
          cd test-bench
          # Connect to the MariaDB service via 127.0.0.1
          bench new-site test.local \
            --db-host 127.0.0.1 \
            --admin-password admin \
            --mariadb-root-password admin
          
          bench --site test.local install-app erpnext
          bench --site test.local install-app india_compliance
          bench --site test.local install-app logi

      - name: Run Tests
        run: |
          cd test-bench
          bench --site test.local run-tests --app logi
